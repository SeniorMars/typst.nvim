= Count Fixture
Plain visible words stay here.
// Hidden comment words vanish.
- List item words remain.
1. Ordered item counts too.
#let hidden = "ignored words"
#set text(size: 10pt)
Math before $ alpha + beta $ after math.
Raw inline `ignored raw words` still counts outside.
```typ
hidden fenced raw words
```
Reference @sec:intro and <label:intro> disappear.
#emph[Function content remains visible]
Visit https://example.com/path now.
