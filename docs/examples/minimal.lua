-- Minimal compile/view workflow with the built-in Typst CLI provider.
require("typst").setup({})

-- First checks after opening a Typst file:
--   :checkhealth typst
--   :TypstInfo
--   :TypstCompile
--   :TypstView
